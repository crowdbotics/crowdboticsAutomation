{
  "name": "crowdbotics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:prd": "npx cypress run --spec cypress/integration/api/tests/*.js --env grep=PRD",
    "test:login": "npx cypress run --spec cypress/integration/api/tests/*.js --env grep=Login",
    "test:settings": "npx cypress run --spec cypress/integration/api/tests/*.js --env grep=Settings",
    "test:dashboard": "npx cypress run --spec cypress/integration/api/tests/*.js --env grep=Dashboard",
    "test:tasks": "npx cypress run --spec cypress/integration/api/tests/*.js --env grep=Tasks"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "btoa": "^1.2.1",
    "cypress-parallel": "^0.13.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "cypress": "^13.2.0",
    "cypress-grep": "^3.0.4",
    "cypress-mochawesome-reporter": "^3.5.1",
    "fs-extra": "^11.1.1",
    "lodash": "^4.17.21",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "or": "^0.2.0"
  }
}
