version: 2.1

executors:
  node:
    docker:
      - image: circleci/node:14.17.0-buster
    working_directory: ~/repo

jobs:
  install_dependencies:
    executor: node
    steps:
      - checkout
      - run:
          name: Install system dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y libnss3 libgtk-3-0 libxss1 libasound2 wget
            wget http://ftp.us.debian.org/debian/pool/main/n/nss/libnss3_3.49.1-1%2bdeb11u1_amd64.deb
            sudo dpkg -i libnss3_3.49.1-1+deb11u1_amd64.deb
            sudo apt-get -f install
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run:
          name: Install Node.js dependencies
          command: npm install
      - run:
          name: Install Cypress
          command: npm install cypress --save-dev
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

  run_tests:
    executor: node
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
      - run:
          name: Run Cypress tests
          command: npm run test:login
      - store_artifacts:
          path: cypress/videos
          destination: videos
      - store_artifacts:
          path: cypress/screenshots
          destination: screenshots
      - store_test_results:
          path: cypress/results

workflows:
  version: 2
  test:
    jobs:
      - install_dependencies
      - run_tests:
          requires:
            - install_dependencies
